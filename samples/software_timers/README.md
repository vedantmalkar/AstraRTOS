## Software timer sample code

timer_a fires every 500 ticks (one-shot), manually restarted on expiry.

timer_b fires every 1000 ticks (repeating), auto-reloads.

The task runs at priority 2 with a stack size of 256.
